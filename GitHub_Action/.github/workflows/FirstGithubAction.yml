name: First Github Actio -Shell commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: echo "Hello, DevSecOps Gurus! This is our First GitHub Action"
